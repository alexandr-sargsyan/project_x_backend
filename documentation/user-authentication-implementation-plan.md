# 📋 План реализации: Система пользователей, лайки и каталоги

**Дата создания:** 2026-01-13  
**Дата обновления:** 2026-01-14  
**Версия:** 1.1  
**Статус:** ✅ Реализация завершена

---

## 📊 Статус реализации

**Все функции реализованы и работают:**

✅ **Backend:**
- Laravel Passport установлен и настроен
- Все миграции созданы и выполнены
- Все модели созданы с правильными связями
- Сервисы для email-кодов работают
- Все контроллеры созданы и работают
- Роуты настроены с правильными middleware
- Email-шаблоны созданы
- Сидер для дефолтного пользователя создан

✅ **Frontend:**
- Контекст аутентификации работает (AuthContext)
- Компоненты аутентификации созданы (модальные окна: LoginModal, RegisterModal, EmailVerificationModal)
- API сервис обновлен с методами для аутентификации, лайков, каталогов
- Компоненты лайков работают (LikeButton с синхронизацией состояния)
- Компоненты каталогов работают (SaveToCollectionButton с модальным окном)
- Страница профиля работает
- Страницы каталогов работают (Collections, CollectionDetail)
- Роутинг настроен
- Навигация обновлена с поддержкой аутентификации

**Особенности реализации:**
- Гибридный подход: просмотр контента доступен всем, действия требуют авторизации
- При попытке действия без авторизации показывается модалка регистрации
- После регистрации сразу показывается форма ввода кода подтверждения
- Лайки обновляются сразу после клика (оптимистичное обновление)
- При попытке добавить видео в каталог, где оно уже есть, ошибка обрабатывается тихо (без alert)

**Последние обновления (2026-01-14):**
- ✅ Все тексты переведены на английский язык (для американского рынка)
- ✅ Добавлены кнопки "Back" на всех страницах (Profile, Collections, CollectionDetail, VideoDetail)
- ✅ Добавлен логотип и название проекта в header (Logo компонент)
- ✅ Улучшена навигация: Logo (слева), SearchBar (центр), Navigation (справа)

---

## 🎯 Обзор задач

### 1. Система аутентификации пользователей
- Регистрация через email + пароль
- Email-подтверждение через код (6-значный код)
- Авторизация через Laravel Passport (OAuth2)
- Email-подтверждение обязательно перед входом

### 2. Система лайков
- Пользователи могут лайкать/убирать лайки с video_references
- Хранение в БД
- API для управления лайками
- Отображение количества лайков

### 3. Система каталогов/коллекций
- Дефолтный каталог "Избранное" при регистрации
- Создание пользовательских каталогов
- Сохранение видео в каталоги
- Одно видео можно добавлять в несколько коллекций
- Дефолтный каталог нельзя переименовывать/удалять
- Только приватные коллекции

### 4. Страница профиля
- Отображение имени пользователя
- Базовая информация

---

## 📦 Технические требования

### Backend
- **Laravel Passport** для OAuth2 аутентификации
- **Email-коды** для подтверждения (6-значный код, хранится в БД)
- **PostgreSQL** для хранения данных

### Frontend
- **React Router** для защищенных роутов
- **Context API** или **React Query** для управления состоянием аутентификации
- Компоненты для регистрации, входа, подтверждения email

---

## 🗄️ Структура базы данных

### Новые таблицы

#### 1. `email_verification_codes`
Хранение кодов подтверждения email.

**Поля:**
- `id` (bigint, PK)
- `email` (string, index) — email пользователя
- `code` (string, 6 символов) — 6-значный код
- `expires_at` (timestamp) — время истечения (15 минут)
- `verified_at` (timestamp, nullable) — когда был подтвержден
- `created_at`, `updated_at` (timestamps)

**Индексы:**
- `email` (index)
- `code` (index)
- `expires_at` (index)

**Логика:**
- Код действителен 15 минут
- После подтверждения код помечается как использованный
- Можно генерировать новый код (старый помечается как неактивный)

#### 2. `video_reference_likes`
Лайки пользователей на видео.

**Поля:**
- `id` (bigint, PK)
- `user_id` (bigint, FK → users.id, onDelete: cascade)
- `video_reference_id` (bigint, FK → video_references.id, onDelete: cascade)
- `created_at`, `updated_at` (timestamps)

**Индексы:**
- Уникальный индекс на `(user_id, video_reference_id)` — один пользователь может лайкнуть видео только один раз
- `user_id` (index)
- `video_reference_id` (index)

#### 3. `video_collections`
Каталоги/коллекции пользователей.

**Поля:**
- `id` (bigint, PK)
- `user_id` (bigint, FK → users.id, onDelete: cascade)
- `name` (string) — название каталога
- `is_default` (boolean, default false) — флаг дефолтного каталога "Избранное"
- `created_at`, `updated_at` (timestamps)

**Индексы:**
- `user_id` (index)
- Уникальный индекс на `(user_id, is_default)` где `is_default = true` — у пользователя может быть только один дефолтный каталог

**Ограничения:**
- Дефолтный каталог нельзя удалять
- Дефолтный каталог нельзя переименовывать (name всегда "Избранное")

#### 4. `video_collection_items`
Связь видео с каталогами (многие-ко-многим).

**Поля:**
- `id` (bigint, PK)
- `collection_id` (bigint, FK → video_collections.id, onDelete: cascade)
- `video_reference_id` (bigint, FK → video_references.id, onDelete: cascade)
- `created_at`, `updated_at` (timestamps)

**Индексы:**
- Уникальный индекс на `(collection_id, video_reference_id)` — одно видео можно добавить в коллекцию только один раз
- `collection_id` (index)
- `video_reference_id` (index)

### Изменения в существующих таблицах

#### `users`
- `email_verified_at` (timestamp, nullable) — уже существует
- Добавить проверку: пользователь не может войти, если `email_verified_at IS NULL`

---

## 🔧 Backend: Пошаговая реализация

### Шаг 1: Установка и настройка Laravel Passport

**Задачи:**
1. Установить пакет `laravel/passport` через Composer
2. Выполнить миграции Passport
3. Настроить Passport в `config/auth.php`
4. Создать клиенты OAuth (personal access client)
5. Настроить модель User для работы с Passport

**Файлы для изменения:**
- `composer.json` — добавить зависимость
- `config/auth.php` — добавить guard для API
- `app/Models/User.php` — добавить trait `HasApiTokens`
- `app/Providers/AppServiceProvider.php` — зарегистрировать Passport

**Команды:**
```bash
composer require laravel/passport
php artisan migrate
php artisan passport:install
```

---

### Шаг 2: Создание миграций для новых таблиц

**Задачи:**
1. Создать миграцию `create_email_verification_codes_table`
2. Создать миграцию `create_video_reference_likes_table`
3. Создать миграцию `create_video_collections_table`
4. Создать миграцию `create_video_collection_items_table`

**Файлы:**
- `database/migrations/YYYY_MM_DD_HHMMSS_create_email_verification_codes_table.php`
- `database/migrations/YYYY_MM_DD_HHMMSS_create_video_reference_likes_table.php`
- `database/migrations/YYYY_MM_DD_HHMMSS_create_video_collections_table.php`
- `database/migrations/YYYY_MM_DD_HHMMSS_create_video_collection_items_table.php`

**Детали миграций:**
- Все внешние ключи с `onDelete('cascade')`
- Уникальные индексы для предотвращения дубликатов
- Индексы для оптимизации запросов

---

### Шаг 3: Создание моделей

**Задачи:**
1. Создать модель `EmailVerificationCode`
2. Создать модель `VideoReferenceLike` (или использовать pivot)
3. Создать модель `VideoCollection`
4. Создать модель `VideoCollectionItem` (или использовать pivot)
5. Обновить модель `User` — добавить связи
6. Обновить модель `VideoReference` — добавить связи

**Файлы:**
- `app/Models/EmailVerificationCode.php`
- `app/Models/VideoCollection.php`
- `app/Models/VideoCollectionItem.php` (опционально, можно использовать pivot)
- `app/Models/User.php` — обновить
- `app/Models/VideoReference.php` — обновить

**Связи:**
- `User` → `EmailVerificationCode` (HasMany)
- `User` → `VideoReferenceLike` (HasMany)
- `User` → `VideoCollection` (HasMany)
- `VideoReference` → `VideoReferenceLike` (HasMany)
- `VideoReference` → `User` (BelongsToMany через likes)
- `VideoCollection` → `User` (BelongsTo)
- `VideoCollection` → `VideoReference` (BelongsToMany через items)
- `VideoReference` → `VideoCollection` (BelongsToMany через items)

---

### Шаг 4: Создание сервисов

**Задачи:**
1. Создать `EmailVerificationService` — генерация и проверка кодов
2. Создать `EmailService` — отправка email с кодом

**Файлы:**
- `app/Services/EmailVerificationService.php`
- `app/Services/EmailService.php` (или использовать Laravel Mail)

**Методы EmailVerificationService:**
- `generateCode(string $email): string` — генерирует 6-значный код
- `sendVerificationCode(string $email): bool` — отправляет код на email
- `verifyCode(string $email, string $code): bool` — проверяет код
- `isCodeExpired(EmailVerificationCode $code): bool` — проверяет истечение
- `markAsVerified(string $email): void` — помечает email как подтвержденный

**Методы EmailService:**
- `sendVerificationCode(string $email, string $code): void` — отправка email

---

### Шаг 5: Создание контроллеров аутентификации

**Задачи:**
1. Создать `AuthController` — регистрация, вход, выход
2. Создать `EmailVerificationController` — отправка и проверка кода

**Файлы:**
- `app/Http/Controllers/AuthController.php`
- `app/Http/Controllers/EmailVerificationController.php`

**Методы AuthController:**
- `register(RegisterRequest $request)` — регистрация (создает пользователя, отправляет код)
- `login(LoginRequest $request)` — вход (проверяет email_verified_at, возвращает токен)
- `logout(Request $request)` — выход (отзывает токен)
- `me(Request $request)` — получить текущего пользователя

**Методы EmailVerificationController:**
- `sendCode(SendCodeRequest $request)` — отправить код на email
- `verifyCode(VerifyCodeRequest $request)` — проверить код и подтвердить email

**Request классы:**
- `app/Http/Requests/RegisterRequest.php`
- `app/Http/Requests/LoginRequest.php`
- `app/Http/Requests/SendCodeRequest.php`
- `app/Http/Requests/VerifyCodeRequest.php`

---

### Шаг 6: Создание контроллера лайков

**Задачи:**
1. Создать `LikeController` — управление лайками

**Файлы:**
- `app/Http/Controllers/LikeController.php`

**Методы:**
- `toggleLike(string $videoReferenceId)` — переключить лайк (добавить/убрать)
- `getUserLikes(Request $request)` — получить все лайки текущего пользователя
- `checkLike(string $videoReferenceId)` — проверить, лайкнул ли пользователь видео

**Обновить VideoReferenceController:**
- В методах `index()` и `show()` добавить информацию о лайках (количество, лайкнул ли текущий пользователь)

---

### Шаг 7: Создание контроллера каталогов

**Задачи:**
1. Создать `VideoCollectionController` — управление каталогами
2. Создать `VideoCollectionItemController` — управление видео в каталогах

**Файлы:**
- `app/Http/Controllers/VideoCollectionController.php`
- `app/Http/Controllers/VideoCollectionItemController.php`

**Методы VideoCollectionController:**
- `index(Request $request)` — список всех каталогов пользователя
- `store(StoreCollectionRequest $request)` — создать каталог
- `show(string $id)` — детали каталога с видео
- `update(UpdateCollectionRequest $request, string $id)` — обновить каталог (нельзя обновлять дефолтный)
- `destroy(string $id)` — удалить каталог (нельзя удалять дефолтный)

**Методы VideoCollectionItemController:**
- `store(AddVideoRequest $request, string $collectionId)` — добавить видео в каталог
- `destroy(string $collectionId, string $videoReferenceId)` — удалить видео из каталога
- `index(string $collectionId)` — список видео в каталоге

**Request классы:**
- `app/Http/Requests/StoreCollectionRequest.php`
- `app/Http/Requests/UpdateCollectionRequest.php`
- `app/Http/Requests/AddVideoRequest.php`

**Логика:**
- При регистрации автоматически создается дефолтный каталог "Избранное"
- Дефолтный каталог нельзя переименовывать (валидация в UpdateCollectionRequest)
- Дефолтный каталог нельзя удалять (проверка в destroy)

---

### Шаг 8: Создание контроллера профиля

**Задачи:**
1. Создать `ProfileController` — управление профилем

**Файлы:**
- `app/Http/Controllers/ProfileController.php`

**Методы:**
- `show(Request $request)` — получить профиль текущего пользователя
- `update(UpdateProfileRequest $request)` — обновить профиль (имя)

**Request классы:**
- `app/Http/Requests/UpdateProfileRequest.php`

---

### Шаг 9: Настройка роутов API

**Задачи:**
1. Добавить роуты для аутентификации
2. Добавить роуты для подтверждения email
3. Добавить роуты для лайков
4. Добавить роуты для каталогов
5. Добавить роуты для профиля
6. Защитить роуты middleware `auth:api`

**Файлы:**
- `routes/api.php`

**Роуты:**
```php
// Публичные роуты (без аутентификации)
POST   /api/register              - регистрация
POST   /api/login                 - вход
POST   /api/email-verification/send-code    - отправить код
POST   /api/email-verification/verify-code  - проверить код

// Защищенные роуты (требуют аутентификации)
POST   /api/logout                - выход
GET    /api/me                     - текущий пользователь
GET    /api/profile                - профиль
PUT    /api/profile                - обновить профиль

// Лайки
POST   /api/video-references/{id}/like      - переключить лайк
GET    /api/video-references/{id}/like      - проверить лайк
GET    /api/likes                           - все лайки пользователя

// Каталоги
GET    /api/collections                     - список каталогов
POST   /api/collections                     - создать каталог
GET    /api/collections/{id}                - детали каталога
PUT    /api/collections/{id}                - обновить каталог
DELETE /api/collections/{id}                - удалить каталог

// Видео в каталогах
GET    /api/collections/{id}/videos         - список видео в каталоге
POST   /api/collections/{id}/videos         - добавить видео
DELETE /api/collections/{id}/videos/{videoId} - удалить видео
```

---

### Шаг 10: Создание Email-шаблонов

**Задачи:**
1. Создать Mailable класс для отправки кода подтверждения
2. Создать Blade шаблон для email

**Файлы:**
- `app/Mail/EmailVerificationMail.php`
- `resources/views/emails/verification-code.blade.php`

**Содержание email:**
- Приветствие
- 6-значный код
- Инструкция по использованию
- Время действия кода (15 минут)

---

### Шаг 11: Middleware для проверки email-подтверждения

**Задачи:**
1. Создать middleware `EnsureEmailVerified` — проверяет, подтвержден ли email

**Файлы:**
- `app/Http/Middleware/EnsureEmailVerified.php`

**Логика:**
- Проверяет `email_verified_at` у пользователя
- Если не подтвержден, возвращает 403 с сообщением
- Применяется к защищенным роутам (кроме отправки/проверки кода)

---

### Шаг 12: Обновление существующих контроллеров

**Задачи:**
1. Обновить `VideoReferenceController` — добавить информацию о лайках в ответы

**Изменения:**
- В `index()` и `show()` добавить:
  - `likes_count` — количество лайков
  - `is_liked` — лайкнул ли текущий пользователь (если авторизован)

---

### Шаг 13: Создание сидера для дефолтного пользователя

**Задачи:**
1. Создать `UserSeeder` — сидер с дефолтным пользователем для разработчиков
2. Обновить `DatabaseSeeder` — добавить вызов `UserSeeder`

**Файлы:**
- `database/seeders/UserSeeder.php` — создать
- `database/seeders/DatabaseSeeder.php` — обновить

**Учетные данные дефолтного пользователя:**
- Email: `developer@example.com`
- Password: `developer`
- Name: `Developer`
- Email подтвержден: `Да` (email_verified_at заполнен)

**Логика:**
- Проверка существования пользователя перед созданием
- Email сразу подтвержден для удобства разработки
- После создания миграций для каталогов можно добавить создание дефолтного каталога "Избранное"

**Команда для запуска:**
```bash
php artisan db:seed --class=UserSeeder
# или
php artisan db:seed
```

---

### Шаг 14: Тестирование Backend

**Задачи:**
1. Протестировать регистрацию
2. Протестировать отправку кода
3. Протестировать проверку кода
4. Протестировать вход
5. Протестировать лайки
6. Протестировать каталоги
7. Протестировать профиль
8. Протестировать вход с дефолтным пользователем

---

## 🎨 Frontend: Пошаговая реализация

### Шаг 1: Установка зависимостей

**Задачи:**
1. Установить необходимые пакеты (если нужно)

**Пакеты (если еще не установлены):**
- `axios` — для HTTP запросов
- `react-router-dom` — для роутинга
- `@tanstack/react-query` — для управления состоянием (уже используется)

---

### Шаг 2: Создание контекста аутентификации

**Задачи:**
1. Создать `AuthContext` для управления состоянием аутентификации
2. Создать `AuthProvider` компонент
3. Создать хук `useAuth`

**Файлы:**
- `src/contexts/AuthContext.js`
- `src/hooks/useAuth.js`

**Функционал:**
- Хранение токена в localStorage
- Хранение информации о пользователе
- Методы: `login`, `logout`, `register`, `sendVerificationCode`, `verifyCode`
- Проверка авторизации

---

### Шаг 3: Настройка Axios interceptors

**Задачи:**
1. Обновить `src/services/api.js` — добавить interceptors для токенов

**Функционал:**
- Добавлять токен в заголовки запросов
- Обрабатывать ошибки 401 (неавторизован) — редирект на логин
- Обрабатывать ошибки 403 (email не подтвержден)

---

### Шаг 4: Создание компонентов аутентификации

**Задачи:**
1. Создать `RegisterForm` — форма регистрации
2. Создать `LoginForm` — форма входа
3. Создать `EmailVerificationForm` — форма подтверждения email
4. Создать `ProtectedRoute` — компонент для защищенных роутов

**Файлы:**
- `src/components/Auth/RegisterForm.jsx`
- `src/components/Auth/RegisterForm.css`
- `src/components/Auth/LoginForm.jsx`
- `src/components/Auth/LoginForm.css`
- `src/components/Auth/EmailVerificationForm.jsx`
- `src/components/Auth/EmailVerificationForm.css`
- `src/components/Auth/ProtectedRoute.jsx`

**Функционал:**
- Валидация форм
- Обработка ошибок
- Редиректы после успешных операций
- Состояния загрузки

---

### Шаг 5: Создание страниц аутентификации

**Задачи:**
1. Создать страницу `Register` — регистрация
2. Создать страницу `Login` — вход
3. Создать страницу `EmailVerification` — подтверждение email

**Файлы:**
- `src/pages/Register.jsx`
- `src/pages/Register.css`
- `src/pages/Login.jsx`
- `src/pages/Login.css`
- `src/pages/EmailVerification.jsx`
- `src/pages/EmailVerification.css`

---

### Шаг 6: Обновление API сервиса

**Задачи:**
1. Добавить методы для аутентификации
2. Добавить методы для лайков
3. Добавить методы для каталогов
4. Добавить методы для профиля

**Файлы:**
- `src/services/api.js` — обновить

**Новые методы:**
```javascript
// Аутентификация
export const register = async (data) => { ... }
export const login = async (data) => { ... }
export const logout = async () => { ... }
export const getCurrentUser = async () => { ... }
export const sendVerificationCode = async (email) => { ... }
export const verifyCode = async (email, code) => { ... }

// Лайки
export const toggleLike = async (videoId) => { ... }
export const checkLike = async (videoId) => { ... }
export const getUserLikes = async () => { ... }

// Каталоги
export const getCollections = async () => { ... }
export const createCollection = async (data) => { ... }
export const getCollection = async (id) => { ... }
export const updateCollection = async (id, data) => { ... }
export const deleteCollection = async (id) => { ... }
export const getCollectionVideos = async (id) => { ... }
export const addVideoToCollection = async (collectionId, videoId) => { ... }
export const removeVideoFromCollection = async (collectionId, videoId) => { ... }

// Профиль
export const getProfile = async () => { ... }
export const updateProfile = async (data) => { ... }
```

---

### Шаг 7: Создание компонентов для лайков

**Задачи:**
1. Создать компонент `LikeButton` — кнопка лайка

**Файлы:**
- `src/components/LikeButton/LikeButton.jsx`
- `src/components/LikeButton/LikeButton.css`

**Функционал:**
- Отображение количества лайков
- Визуальная индикация (лайкнуто/не лайкнуто)
- Обработка клика (переключение лайка)
- Только для авторизованных пользователей

---

### Шаг 8: Интеграция лайков в существующие компоненты

**Задачи:**
1. Добавить `LikeButton` в `VideoCard`
2. Добавить `LikeButton` в `VideoDetailView`
3. Обновить API запросы для получения информации о лайках

**Файлы:**
- `src/components/VideoCard/VideoCard.jsx` — обновить
- `src/components/VideoDetailView/VideoDetailView.jsx` — обновить

---

### Шаг 9: Создание компонентов для каталогов

**Задачи:**
1. Создать `CollectionsList` — список каталогов
2. Создать `CollectionCard` — карточка каталога
3. Создать `CollectionForm` — форма создания/редактирования каталога
4. Создать `AddToCollectionModal` — модальное окно для добавления видео в каталог
5. Создать `CollectionVideosList` — список видео в каталоге

**Файлы:**
- `src/components/Collections/CollectionsList.jsx`
- `src/components/Collections/CollectionsList.css`
- `src/components/Collections/CollectionCard.jsx`
- `src/components/Collections/CollectionCard.css`
- `src/components/Collections/CollectionForm.jsx`
- `src/components/Collections/CollectionForm.css`
- `src/components/Collections/AddToCollectionModal.jsx`
- `src/components/Collections/AddToCollectionModal.css`
- `src/components/Collections/CollectionVideosList.jsx`
- `src/components/Collections/CollectionVideosList.css`

**Функционал:**
- Создание каталога
- Редактирование каталога (кроме дефолтного)
- Удаление каталога (кроме дефолтного)
- Добавление видео в каталог
- Удаление видео из каталога
- Просмотр видео в каталоге

---

### Шаг 10: Создание страниц для каталогов

**Задачи:**
1. Создать страницу `Collections` — список всех каталогов
2. Создать страницу `CollectionDetail` — детали каталога с видео

**Файлы:**
- `src/pages/Collections.jsx`
- `src/pages/Collections.css`
- `src/pages/CollectionDetail.jsx`
- `src/pages/CollectionDetail.css`

---

### Шаг 11: Создание страницы профиля

**Задачи:**
1. Создать страницу `Profile` — профиль пользователя

**Файлы:**
- `src/pages/Profile.jsx`
- `src/pages/Profile.css`

**Функционал:**
- Отображение имени пользователя
- Отображение email
- Форма редактирования имени
- Базовая информация

---

### Шаг 12: Обновление навигации

**Задачи:**
1. Добавить ссылки на страницы аутентификации (если не авторизован)
2. Добавить ссылки на профиль, каталоги (если авторизован)
3. Добавить кнопку выхода

**Файлы:**
- Создать компонент `Navigation` или обновить существующий
- `src/components/Navigation/Navigation.jsx`
- `src/components/Navigation/Navigation.css`

---

### Шаг 13: Обновление роутинга

**Задачи:**
1. Добавить роуты для новых страниц
2. Защитить роуты через `ProtectedRoute`

**Файлы:**
- `src/App.js` — обновить

**Новые роуты:**
```javascript
/register              - регистрация
/login                 - вход
/email-verification    - подтверждение email
/profile               - профиль (защищенный)
/collections           - список каталогов (защищенный)
/collections/:id       - детали каталога (защищенный)
```

---

### Шаг 14: Добавление кнопки "Сохранить в коллекцию"

**Задачи:**
1. Добавить кнопку в `VideoCard` (опционально)
2. Добавить кнопку в `VideoDetailView`
3. При клике открывать `AddToCollectionModal`

**Файлы:**
- `src/components/VideoCard/VideoCard.jsx` — обновить
- `src/components/VideoDetailView/VideoDetailView.jsx` — обновить

---

### Шаг 15: Тестирование Frontend

**Задачи:**
1. Протестировать регистрацию
2. Протестировать подтверждение email
3. Протестировать вход
4. Протестировать лайки
5. Протестировать каталоги
6. Протестировать профиль

---

## 🔄 Последовательность реализации

### Фаза 1: Backend - Аутентификация (Шаги 1-5, 9-11, 13)
1. Установка Passport
2. Миграции для email-кодов
3. Модели и сервисы
4. Контроллеры аутентификации
5. Роуты и middleware
6. Сидер для дефолтного пользователя

### Фаза 2: Backend - Лайки (Шаги 2, 3, 6, 12)
1. Миграция для лайков
2. Модели и связи
3. Контроллер лайков
4. Обновление VideoReferenceController

### Фаза 3: Backend - Каталоги (Шаги 2, 3, 7)
1. Миграции для каталогов
2. Модели и связи
3. Контроллеры каталогов

### Фаза 4: Backend - Профиль (Шаг 8)
1. Контроллер профиля

### Фаза 5: Frontend - Аутентификация (Шаги 1-5, 13)
1. Контекст аутентификации
2. Компоненты и страницы
3. Роутинг

### Фаза 6: Frontend - Лайки (Шаги 6-8)
1. API методы
2. Компонент LikeButton
3. Интеграция

### Фаза 7: Frontend - Каталоги (Шаги 6, 9-11, 14)
1. API методы
2. Компоненты каталогов
3. Страницы
4. Интеграция

### Фаза 8: Frontend - Профиль (Шаги 6, 11, 12)
1. API методы
2. Страница профиля
3. Навигация

---

## ✅ Чеклист готовности

### Backend
- [x] Laravel Passport установлен и настроен
- [x] Миграции созданы и выполнены
- [x] Модели созданы с правильными связями
- [x] Сервисы для email-кодов работают
- [x] Контроллеры созданы и протестированы
- [x] Роуты настроены
- [x] Middleware работает
- [x] Email-шаблоны созданы
- [x] Сидер для дефолтного пользователя создан
- [x] Все API endpoints работают

### Frontend
- [x] Контекст аутентификации работает
- [x] Компоненты аутентификации созданы (модальные окна)
- [x] Страницы аутентификации созданы (интегрированы в модальные окна)
- [x] API сервис обновлен
- [x] Компоненты лайков работают
- [x] Компоненты каталогов работают
- [x] Страница профиля работает
- [x] Роутинг настроен
- [x] Навигация обновлена
- [x] Все функции протестированы

---

## 📝 Примечания

1. **Email-коды:**
   - Код действителен 15 минут
   - После использования код помечается как использованный
   - Можно запросить новый код (старый становится неактивным)

2. **Дефолтный каталог:**
   - Создается автоматически при регистрации
   - Название всегда "Избранное"
   - Нельзя удалять или переименовывать
   - Проверка в контроллере и валидации

3. **Лайки:**
   - Один пользователь может лайкнуть видео только один раз
   - Уникальный индекс предотвращает дубликаты
   - При удалении видео или пользователя лайки удаляются каскадно

4. **Каталоги:**
   - Одно видео можно добавить в несколько каталогов
   - Уникальный индекс на (collection_id, video_reference_id)
   - При удалении каталога все видео удаляются из него

5. **Безопасность:**
   - Все защищенные роуты требуют токен
   - Email должен быть подтвержден для доступа к защищенным роутам
   - Пользователь может видеть только свои каталоги

---

**Дата создания документа:** 2026-01-13  
**Дата обновления:** 2026-01-13  
**Версия:** 1.0  
**Статус:** ✅ Реализация завершена

